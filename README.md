# GitLab Container Registry

GitLab Container Registry is a built-in container registry that allows users to store Docker images alongside their code in GitLab repositories. It exposes a REST API for managing image repositories, tags, and cleanup policies.

## APIs

- [GitLab Container Registry](https://docs.gitlab.com/user/packages/container_registry/) - Container image repositories tightly integrated with GitLab projects and groups.

## Properties

| Property | URL |
|----------|-----|
| Documentation | https://docs.gitlab.com/user/packages/container_registry/ |
| API Documentation | https://docs.gitlab.com/api/container_registry/ |
| OpenAPI | openapi/gitlab-container-registry-openapi.yml |
| Capabilities | capabilities/gitlab-container-registry-capabilities.yml |
| Rules | rules/gitlab-container-registry-rules.yml |

## Tags

Container Images, Containers, GitLab, Registry

## Maintainers

- **Kin Lane** - kin@apievangelist.com
